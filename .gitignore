# Machine-local Claude Code permissions; never share across team members.
settings.local.json
.claude/settings.local.json

# Workflow owner mirror files (machine-local, generated by workflow-owner.ps1).
**/.workflow-owner.json

# Workflow runtime state & handoff (machine-local, generated by workflow-state.ps1).
**/00_runtime.json
**/handoff.json

# Guard manual switch file (machine-local runtime toggle).
.guard-disabled

# Temporary test and staging files
.ai-workspace-test/
.ai-sop-staging/
review-mailbox.json
**/review-mailbox.json
*.tmp
*.lock
*.log

# OS metadata
.DS_Store
Thumbs.db
